# Loaded automatically when running ansible-playbook from this directory.
# YAML result rendering keeps multi-line output (cluster status, next-steps
# commands, preflight reports) human-readable and copy-paste friendly,
# instead of the default JSON list-of-quoted-lines.
[defaults]
callback_result_format = yaml
